# SPDX-FileCopyrightText: 2023 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2023-2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Ensure Prometheus Alertmanager paths exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0750"
    owner: "{{ prometheus_alertmanager_uid }}"
    group: "{{ prometheus_alertmanager_gid }}"
  with_items:
    - "{{ prometheus_alertmanager_base_path }}"
    - "{{ prometheus_alertmanager_config_path }}"
    - "{{ prometheus_alertmanager_data_path }}"
    - "{{ prometheus_alertmanager_templates_path }}"

- name: Ensure Prometheus Alertmanager support files installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/{{ item }}.j2"
    dest: "{{ prometheus_alertmanager_base_path }}/{{ item }}"
    mode: "0640"
    owner: "{{ prometheus_alertmanager_uid }}"
    group: "{{ prometheus_alertmanager_gid }}"
  with_items:
    - env
    - labels
  register: prometheus_alertmanager_support_files_result

- name: Ensure Prometheus Alertmanager configuration file installed
  ansible.builtin.copy:
    content: "{{ prometheus_alertmanager_configuration | to_nice_yaml(indent=2, width=999999) }}"
    dest: "{{ prometheus_alertmanager_config_path }}/config.yml"
    mode: "0644"
    owner: "{{ prometheus_alertmanager_uid }}"
    group: "{{ prometheus_alertmanager_gid }}"
  register: prometheus_alertmanager_config_file_result

- name: Ensure Prometheus Alertmanager container image is pulled via community.docker.docker_image
  when: devture_systemd_docker_base_container_image_pull_method == 'ansible-module'
  community.docker.docker_image:
    name: "{{ prometheus_alertmanager_container_image }}"
    source: "{{ 'pull' if ansible_version.major > 2 or ansible_version.minor > 7 else omit }}"
    force_source: "{{ prometheus_alertmanager_container_image_force_pull if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
    force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else prometheus_alertmanager_container_image_force_pull }}"
  register: prometheus_alertmanager_container_image_pull_result
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: prometheus_alertmanager_container_image_pull_result is not failed

- name: Ensure Prometheus Alertmanager container image is pulled via ansible.builtin.command
  when: devture_systemd_docker_base_container_image_pull_method == 'command'
  ansible.builtin.command:
    cmd: "{{ devture_systemd_docker_base_host_command_docker }} pull {{ prometheus_alertmanager_container_image }}"
  register: prometheus_alertmanager_container_image_pull_result
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: prometheus_alertmanager_container_image_pull_result is not failed
  changed_when: "'Downloaded newer image' in prometheus_alertmanager_container_image_pull_result.stdout"

- name: Ensure Prometheus Alertmanager container network is created via community.docker.docker_network
  when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
  community.docker.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ prometheus_alertmanager_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"
  register: prometheus_alertmanager_network_creation_result

- name: Ensure Prometheus Alertmanager container network is created via ansible.builtin.command
  when: devture_systemd_docker_base_container_network_creation_method == 'command'
  ansible.builtin.command:
    cmd: >-
      {{ devture_systemd_docker_base_host_command_docker }} network create
      {% if devture_systemd_docker_base_ipv6_enabled %}--ipv6{% endif %}
      {{ devture_systemd_docker_base_container_networks_driver_options_string }}
      {{ prometheus_alertmanager_container_network }}
  register: prometheus_alertmanager_network_creation_result
  changed_when: prometheus_alertmanager_network_creation_result.rc == 0
  failed_when: prometheus_alertmanager_network_creation_result.rc != 0 and 'already exists' not in prometheus_alertmanager_network_creation_result.stderr

- name: Ensure Prometheus Alertmanager systemd service is present
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/prometheus-alertmanager.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/{{ prometheus_alertmanager_identifier }}.service"
    mode: "0644"
  register: prometheus_alertmanager_systemd_service_result

- name: Determine whether Prometheus Alertmanager needs a restart
  ansible.builtin.set_fact:
    prometheus_alertmanager_restart_necessary: >-
      {{
        prometheus_alertmanager_support_files_result.changed | default(false)
        or prometheus_alertmanager_config_file_result.changed | default(false)
        or prometheus_alertmanager_systemd_service_result.changed | default(false)
        or prometheus_alertmanager_container_image_pull_result.changed | default(false)
      }}
